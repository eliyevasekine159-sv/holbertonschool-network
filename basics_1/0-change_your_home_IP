#!/usr/bin/env bash
# Configures local IP resolution for localhost and facebook.com
# hosts faylında localhost sətirini tam təhlükəsiz şəkildə 127.0.0.2 edirik
sed -i 's/\b127\.0\.0\.1\b/127.0.0.2/g' /etc/hosts

# facebook.com yönləndirməsini əlavə edirik (əgər yoxdursa)
if ! grep -q "facebook.com" /etc/hosts; then
    echo "8.8.8.8 facebook.com" >> /etc/hosts
fi
